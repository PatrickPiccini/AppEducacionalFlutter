

import 'package:flutter/material.dart';

class Notificacoes extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
          title: Text('Notificações')
      ),
      body: Text('Estamos Notificações!!!'),
    );
  }
}