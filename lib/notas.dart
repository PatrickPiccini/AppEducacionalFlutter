

import 'package:flutter/material.dart';

class Notas extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
          title: Text('Notas e Faltas')
      ),
      body: Text('Estamos em Notas e Faltas!!!'),
    );
  }
}