

import 'package:flutter/material.dart';

class Horarios extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
          title: Text('Horários')
      ),
      body: Text('Estamos nos Horários!!!'),
    );
  }
}