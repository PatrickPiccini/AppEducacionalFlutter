

import 'package:flutter/material.dart';

class Avaliacao extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
          title: Text('Avaliações')
      ),
      body: Text('Estamos nas Avaliações!!!'),
    );
  }
}